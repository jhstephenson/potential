<%= form_for(@top_fife) do |f| %>
  <% if @top_fife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@top_fife.errors.count, "error") %> prohibited this top_fife from being saved:</h2>

      <ul>
      <% @top_fife.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :opportunity %><br>
    <%= f.text_field :opportunity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
