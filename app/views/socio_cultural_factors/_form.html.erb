<%= form_for(@socio_cultural_factor) do |f| %>
  <% if @socio_cultural_factor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@socio_cultural_factor.errors.count, "error") %> prohibited this socio_cultural_factor from being saved:</h2>

      <ul>
      <% @socio_cultural_factor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :factor %><br>
    <%= f.text_field :factor %>
  </div>
  <div class="field">
    <%= f.label :opportunity %><br>
    <%= f.text_field :opportunity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
